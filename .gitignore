/build/
.idea
.vscode
.vs
.cache
.DS_Store
*.xcuserstate
.codex
.claude
cmake-build**
/bin/
/obj/
.omo/
build-egl/

# mcpp build outputs (the mcpp leg of the dual build system).
target/
.mcpp/
compile_commands.json
# GCC writes module BMIs beside the compiler's working directory; a stray
# `gcm.cache/` at the repo root is a 28 MB build artifact, not a source file.
gcm.cache/
# HuxerUI's profiling writes a runtime trace per process into the working
# directory, so `mcpp test` leaves one behind in the test package.
traces/
__pycache__/
*.pyc

# Working design notes for agents; never committed.
.agents/

# mcpp resolution records; every dependency here is pinned exactly in its manifest.
mcpp.lock
